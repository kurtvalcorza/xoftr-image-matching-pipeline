# Release status

Current status: **Candidate** — the `E2E` tutorial notebook `tutorials/xoftr_image_matching_colab.ipynb` has not yet executed top-to-bottom in a clean supported runtime. The pipeline package with its vendored network and adaptation contract, the offline unit suite, the model-backed regressions (run CPU-only on the build workstation), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), the static validator (`tools/validate_release_assets.py`), the generator parity checks and the CI workflow exist and are green, and the default tutorial path has been run on the build workstation's CPU through the package API (`docs/release-verification.md`, pre-flight row). None of that is runtime evidence: the registry moves to **Release-grade** only when a clean Kaggle / Colab execution of the committed notebook blob is recorded in `docs/release-verification.md`, and any later change to the carried modules or to the notebook yields a new blob and returns it to **Candidate**.
